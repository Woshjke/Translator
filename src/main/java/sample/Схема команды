Будет класс Translator (Receiver).
У него будут методы translate() и, к примеру, dictionary()

Далее идет интерфейс Command
У него будет метод execute()

Этот интерфейс реализовываю классы TranslateCommand и DictionaryCommand
У них будет ссылка на объект класса Translate (Translate translator;)
Ссылка будет инициализироваться через констуктор.
И в методе execute() будут вызываться методы translate() и dictionary() класса Translate. (translator.translate() и тд.)

Будер реализован класс User (Invoker)
Его полями будут ссылки на интерфейс Command (Отдельная ссылка под отдельную комманду)
В конструктор буду тпередаваться нужные команды
Будут реализованы методы translateText() и getInfoFromDictionary(),
в которых будут выхываться методы execute() соответствующих команд.

Так же в этом классе будет реализован опрос пользователя через консоль.
Исходя из ввода будут вызываться соответствующие методы.

Enum - для кодов ответа из API



•	иерархия классов и интерфейсов
•	Коллекции
•	обработка исключений
•	Enum - будет
•	работа с потоками ввода/вывода и файлами - запись логов в файл
•	больше одного потока в приложении




